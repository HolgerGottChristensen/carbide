use crate::{Point, Scalar};
use crate::draw::Position;
use crate::text::InnerGlyph;
use crate::text::text_style::TextStyle;
use crate::widget::{Environment, GlobalState};

pub type Glyph = InnerGlyph;